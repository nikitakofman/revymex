"use client";

import React from "react";
import PreviewPlay from "./components/preview/previewplay";

type Props = {};

const page = (props: Props) => {
  return <PreviewPlay />;
};

export default page;
